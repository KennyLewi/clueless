export default function ProfilePage() {
  return (
    <div className="wrap">
      <h1 className="page-title">Profile</h1>
      <div className="page-sub">Basics, your voice, and writing samples the agent reuses on every form.</div>
      <div style={{ textAlign: "center", padding: "70px 20px", color: "var(--muted)", marginTop: 22 }}>
        <div style={{ fontWeight: 600, color: "var(--text)", marginBottom: 6 }}>Coming in the next pass</div>
        Profile basics + voice onboarding (§6.3.3–6.3.4) are next on the list.
      </div>
    </div>
  );
}
