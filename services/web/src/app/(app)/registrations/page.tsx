export default function RegistrationsPage() {
  return (
    <div className="wrap">
      <h1 className="page-title">Registrations</h1>
      <div className="page-sub">Track every run the agent has started for you.</div>
      <div style={{ textAlign: "center", padding: "70px 20px", color: "var(--muted)", marginTop: 22 }}>
        <div style={{ fontWeight: 600, color: "var(--text)", marginBottom: 6 }}>Coming in the next pass</div>
        The registrations dashboard (status pills + deep links into the autofill states) is next on the list.
      </div>
    </div>
  );
}
